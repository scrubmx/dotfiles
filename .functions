#
# Create a new file change permissions and open in Sublime Text
#
function create(){
    touch $1; chmod 775 $1; subl $1
}


#
# Create new directory and cd into it
#
function mkd(){
    mkdir -p "$@" && eval cd "\"\$$#\"";
}


#
# Filter processes with keyword
#
function showps () {
	ps -A | grep -v 'grep' | grep -i "$@"
}


# 
# Quick show contents of sqlite3 database
#
function get() {
    sqlite3 production.sqlite "SELECT * FROM $@"
}
